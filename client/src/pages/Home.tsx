import {FC} from 'react';
import {getTokenFromLocalStorage} from "../helpers/localstorage.helper";

const Home: FC = () => {
    return (
        <div>
            Home
        </div>
    );
};

export default Home;
